<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<input type="text" id="num1"/> * <input type="text" id="num2">
	<button id="btn"> = </button>
	<input type="text" name="total" id="totnum"/>
</body>
<script type="text/javascript">
	//버튼을 클릭하면 첫번째 필드의 입력값과 두번째 필드의 입력값을 읽음
	//세번째 필드의 두 필드값의 곱을 출력
	
	document.getElementById("btn").addEventListener('click',function(){
		var fields = document.getElementsByName("num");
		var totalNum = 0;
		for(for i = 0; i<fields.length;i++){
			no = fields[i].value;
			totalNum += parseInt(no);
		}
		document.getElementById('totnum').value = totalNum;
	});
	
</script>
</html>